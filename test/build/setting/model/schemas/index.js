"use strict";

module.exports = function (Schema) {
	var ListSchemas = {
		Cat: new Schema({
			name: { type: String, require: true },
			age: { type: Number, require: true }
		}) //last schema

	} //List of schemas

	;
} //end schemas
;